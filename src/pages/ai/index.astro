---
import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';
const articles = (await getCollection('articles', ({data}) => data.category === 'ai'))
  .sort((a,b) => b.data.pubDate.getTime() - a.data.pubDate.getTime());
---
<BaseLayout title="AI & LLM — Zero Click Journal" description="Technical analysis of large language models, AI architectures, and enterprise AI adoption.">
  <div class="home-body">
    <div style="max-width:1060px;margin:0 auto">
      <div class="cat-header">
        <div class="cat-label mono">AI & LLM</div>
        <h1 class="cat-h1">AI & LLM</h1>
        <p class="cat-desc">Technical analysis of large language models, AI architectures, and enterprise AI adoption.</p>
      </div>
      <div class="cat-list">
        {articles.map((e, i) => (
          <a href={`/ai/{e.slug.split('/').pop()}`} class="cat-item">
            <span class="cat-num mono">{String(i+1).padStart(2,'0')}</span>
            <div class="cat-body">
              <div class="cat-title">{e.data.title}</div>
              <div class="cat-excerpt">{e.data.description}</div>
              <div class="cat-date mono">{e.data.pubDate.toISOString().slice(0,10)}</div>
            </div>
            <span class="cat-arrow">→</span>
          </a>
        ))}
      </div>
    </div>
  </div>
</BaseLayout>
