---
import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';

const articles = (await getCollection('articles', ({ data }) => data.category === 'seo'))
  .sort((a, b) => b.data.pubDate.getTime() - a.data.pubDate.getTime());
---
<BaseLayout
  title="Digital Health"
  description="In-depth coverage of AI-powered diagnostics, digital therapeutics, and healthcare technology."
  pageType="collection"
>
  <div class="home-body">
    <div style="max-width:1060px;margin:0 auto">
      <div class="cat-header">
        <div class="cat-label mono">Digital Health</div>
        <h1 class="cat-h1">Digital Health</h1>
        <p class="cat-desc">In-depth coverage of AI-powered diagnostics, digital therapeutics, and healthcare technology.</p>
      </div>
      <div class="cat-list">
        {articles.map((e, i) => (
          <a href={`/seo/${e.slug.split('/').pop()}/`} class="cat-item">
            <span class="cat-num mono">{String(i + 1).padStart(2, '0')}</span>
            <div class="cat-body">
              <div class="cat-title">{e.data.title}</div>
              <div class="cat-excerpt">{e.data.description}</div>
              <div class="cat-date mono">{e.data.pubDate.toISOString().slice(0, 10)}</div>
            </div>
            <span class="cat-arrow">→</span>
          </a>
        ))}
      </div>
    </div>
  </div>
</BaseLayout>
